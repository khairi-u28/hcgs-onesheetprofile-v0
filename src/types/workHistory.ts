export type WorkHistoryRecord = {
  nrp: string;
  position: string;
  pos?: string;
  branchCode: string;
  branchName: string;
  startDate: string; // ISO string
  endDate: string; // ISO string
};
